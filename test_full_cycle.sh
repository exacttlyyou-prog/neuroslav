#!/bin/bash
# Скрипт для тестирования полного цикла обработки meeting-notes

echo "🧪 Тестирование полного цикла обработки meeting-notes"
echo ""

# Проверка статуса парсера
echo "1️⃣ Проверка статуса фонового парсера:"
curl -s "http://localhost:8000/api/notion/parser/status" | python3 -m json.tool
echo ""

# Ручная проверка последнего блока (запускает полный цикл)
echo "2️⃣ Ручная проверка последнего блока (запускает полный цикл обработки):"
curl -X POST "http://localhost:8000/api/notion/parser/check" \
  -H "Content-Type: application/json" \
  -s | python3 -m json.tool
echo ""

# Альтернатива: через API endpoint с обработкой
echo "3️⃣ Альтернатива: через API endpoint с обработкой:"
curl -X POST "http://localhost:8000/api/notion/last-meeting/auto?process=true" \
  -H "Content-Type: application/json" \
  -s | python3 -m json.tool | head -50
echo ""

echo "✅ Тестирование завершено. Проверьте логи сервера для деталей."
