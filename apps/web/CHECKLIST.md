# Чеклист проверки работы приложения

## ✅ Базовая проверка

- [ ] Dev server запущен: `npm run dev` в директории `apps/web`
- [ ] В терминале видно: `✓ Ready in Xs` и `○ Local: http://localhost:3000`
- [ ] Браузер открыт на http://localhost:3000

## 🔍 Если страница не отображается

### 1. Проверьте консоль браузера (F12 → Console)
   - Должно быть пусто или только информационные сообщения
   - Если есть ошибки (красным) - скопируйте их

### 2. Проверьте Network tab (F12 → Network)
   - Перезагрузите страницу (Cmd+R / Ctrl+R)
   - Все запросы должны быть зелеными (200, 304)
   - Если есть красные (404, 500) - это проблема

### 3. Проверьте, что видно в браузере
   - Должны быть видны табы: "Task Reminder", "Meeting Dump", "Context/RAG"
   - Должна быть форма с полем ввода
   - Должны применяться стили (не просто текст)

## 🐛 Частые проблемы

### Проблема: Белый экран
**Решение**: Откройте консоль (F12) и проверьте ошибки JavaScript

### Проблема: Стили не применяются
**Решение**: 
```bash
cd apps/web
rm -rf .next
npm run dev
```

### Проблема: "Cannot find module"
**Решение**:
```bash
cd apps/web
rm -rf node_modules .next
npm install
npm run dev
```

### Проблема: Порт занят
**Решение**: Измените порт в `package.json`:
```json
"dev": "next dev -p 3001"
```

## 📸 Что должно быть видно

1. **Заголовок**: "Task Reminder"
2. **Описание**: "Введите задачу, и система автоматически определит deadline и напомнит вам"
3. **Поле ввода** с placeholder: "Например: Напомни мне про встречу в следующий вторник"
4. **Кнопка**: "Создать задачу"
5. **Второй блок**: "Список задач"

## 🚀 Если все работает

Попробуйте:
1. Ввести текст в поле и нажать "Создать задачу"
2. Переключиться на другие табы
3. Проверить, что формы отображаются на всех табах

## 📝 Отчет об ошибках

Если что-то не работает, укажите:
1. Что именно не работает (белый экран, ошибка, форма не отправляется)
2. Текст ошибки из консоли браузера (F12)
3. Скриншот (если возможно)
