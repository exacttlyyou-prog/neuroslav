# Цикл работы мульти-агентной системы обработки встреч

## Общий обзор

Система автоматически обрабатывает AI Meeting Notes из Notion, извлекая задачи и решения, которые затем создаются в базе данных задач.

## Архитектура: Паттерн "Orchestrator-Workers"

Система состоит из **координатора** (главный цикл) и **трех специализированных агентов**:

```
┌─────────────────┐
│  Coordinator    │  ← Главный цикл (опрашивает БД, управляет состоянием)
└────────┬────────┘
         │
         ├──→ Scraper Agent  (Playwright) → извлекает контент
         │
         ├──→ Analyst Agent  (OpenAI)     → структурирует данные
         │
         └──→ Writer Agent   (Notion API) → создает задачи
```

## Детальный цикл работы

### Шаг 1: Опрос базы данных (Coordinator)

**Что происходит:**
- Координатор каждую минуту (или по настройке `POLL_INTERVAL`) опрашивает базу данных Notion со встречами
- Ищет страницы, у которых свойство `Status` (или указанное в `NOTION_STATUS_PROPERTY`) равно **"Ready to Process"**

**Где:** `coordinator.ts` → функция `findPagesToProcess()`

---

### Шаг 2: Начало обработки страницы

**Что происходит:**
- Когда найдена страница со статусом "Ready to Process":
  1. Координатор **сразу обновляет статус** на **"Processing"** (чтобы другие процессы не взяли ту же страницу)
  2. Формирует URL страницы Notion
  3. Запускает цепочку агентов

**Где:** `coordinator.ts` → функция `processPage()`

---

### Шаг 3: Извлечение контента (Scraper Agent)

**Что происходит:**
- **Scraper Agent** использует **Playwright** для открытия страницы в браузере
- Загружает сохраненное состояние сессии из `auth.json` (автоматическая авторизация)
- Выполняет **"Smart Scroll"** — умную прокрутку страницы для загрузки виртуализированного контента Notion
- Ищет блоки с текстом "AI Summary", "Summary", "Саммари" или "Резюме"
- Извлекает весь текст саммари и транскрипции

**Особенности:**
- Использует **retry механизм** (до 3 попыток с экспоненциальной задержкой)
- Если точный поиск не дал результатов, извлекает весь контент страницы (fallback)

**Выход:** Сырой текст (raw string)

**Где:** `agents/scraper.ts` → функции `scrapeMeetingContent()` и `scrapeWithRetry()`

---

### Шаг 4: Анализ и структурирование (Analyst Agent)

**Что происходит:**
- **Analyst Agent** отправляет сырой текст в **OpenAI** (модель `gpt-4o-mini`)
- Использует специальный промпт для извлечения структурированных данных
- LLM анализирует текст и возвращает JSON с:
  - **Action Items** (задачи) — массив объектов с полями: `title`, `description`, `assignee`, `dueDate`, `priority`
  - **Key Decisions** (ключевые решения) — массив объектов с полями: `title`, `description`, `context`
  - **Summary** (опционально) — краткое резюме встречи

**Выход:** Структурированный JSON объект (`StructuredMeetingData`)

**Где:** `agents/analyst.ts` → функция `analyzeMeetingContent()`

---

### Шаг 5: Запись в Notion (Writer Agent)

**Что происходит:**
- **Writer Agent** получает структурированные данные от Analyst
- **Создает задачи** в базе данных задач (`NOTION_TASKS_DB_ID`):
  - Для каждой задачи из `actionItems` создает новую страницу в БД задач
  - Заполняет свойства: `Name`, `Description`, `Assignee`, `Due Date`, `Priority`
- **Обновляет исходную страницу встречи**:
  - Добавляет заголовок "📋 Созданные задачи"
  - Добавляет ссылки на созданные задачи
  - Если есть `summary`, добавляет его как callout блок

**Выход:** Массив ID созданных задач

**Где:** `agents/writer.ts` → функции `createTasks()` и `updateMeetingPage()`

---

### Шаг 6: Завершение обработки

**Что происходит:**
- Если все шаги прошли успешно:
  - Координатор обновляет статус страницы на **"Done"**
  - Логирует успешное завершение
- Если на любом этапе произошла ошибка:
  - Координатор обновляет статус на **"Error"**
  - Логирует ошибку для отладки
  - Продолжает обработку следующих страниц

**Где:** `coordinator.ts` → функция `processPage()` (try-catch блок)

---

## Полный цикл в виде диаграммы

```
1. Coordinator опрашивает БД
   ↓
2. Находит страницу "Ready to Process"
   ↓
3. Обновляет статус → "Processing"
   ↓
4. Scraper Agent (Playwright)
   ├─ Открывает страницу
   ├─ Smart Scroll
   ├─ Извлекает текст "AI Summary"
   └─ Возвращает raw text
   ↓
5. Analyst Agent (OpenAI)
   ├─ Анализирует текст
   ├─ Извлекает задачи и решения
   └─ Возвращает JSON
   ↓
6. Writer Agent (Notion API)
   ├─ Создает задачи в БД
   └─ Обновляет страницу встречи
   ↓
7. Coordinator обновляет статус → "Done"
   ↓
8. Возврат к шагу 1 (опрос БД)
```

## Управление состоянием

Система использует **state machine** для управления жизненным циклом страницы:

```
"Ready to Process" → "Processing" → "Done" ✅
                              ↓
                           "Error" ❌
```

- **"Ready to Process"** — страница готова к обработке
- **"Processing"** — страница обрабатывается (защита от дублирования)
- **"Done"** — обработка успешно завершена
- **"Error"** — произошла ошибка (можно повторить вручную, изменив статус обратно на "Ready to Process")

## Обработка ошибок

- **Scraper:** Retry механизм (3 попытки с экспоненциальной задержкой)
- **Analyst:** Валидация JSON, fallback на пустые массивы
- **Writer:** Продолжает создавать задачи даже если одна не удалась
- **Coordinator:** Логирует ошибки, обновляет статус на "Error", продолжает работу

## Настройка

Все настройки в `.env`:
- `POLL_INTERVAL` — интервал опроса (по умолчанию 60000 мс = 1 минута)
- `NOTION_STATUS_PROPERTY` — название свойства статуса (по умолчанию "Status")
- `AUTH_FILE_PATH` — путь к `auth.json` (по умолчанию `../../auth.json`)

## Запуск

Система запускается автоматически при старте Digital Twin через `launch.sh`:
- Проверяет зависимости
- Компилирует TypeScript код
- Запускает в фоновом режиме
- Логи пишутся в `/tmp/digital_twin_agent_worker.log`
