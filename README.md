# 🤖 Digital Twin System

Персональный движок обработки хаоса, который превращает ежедневные входные данные (задачи, встречи, документы) в структурированные действия, резюме и знания.

## 📚 Документация

- **[ROADMAP.md](./ROADMAP.md)** — Детальный план реализации по фазам
- **[ARCHITECTURE.md](./ARCHITECTURE.md)** — Архитектура системы и компонентов

## 🏗️ Структура проекта

```
digital-twin/
├── apps/
│   ├── web/              # Next.js Frontend
│   └── api/               # Python FastAPI Backend (опционально)
├── packages/            # Shared packages
├── data/                # Локальные данные
├── scripts/             # Утилиты
└── docs/                # Документация
```

## 🚀 Быстрый старт

### Требования

- Node.js 18+
- Python 3.11+
- pnpm (рекомендуется) или npm

### Установка

```bash
# Установить зависимости Frontend
cd apps/web
pnpm install

# Установить зависимости Backend (если используется)
cd apps/api
pip install -r requirements.txt
```

### Настройка окружения

Скопируйте `.env.example` в `.env` и заполните переменные:

```bash
# OpenAI
OPENAI_API_KEY=your_key_here

# Telegram Bot
TELEGRAM_BOT_TOKEN=your_token_here
TELEGRAM_CHAT_ID=your_chat_id

# Vector DB (опционально)
PINECONE_API_KEY=your_key_here
```

### Запуск

```bash
# Frontend (Next.js)
cd apps/web
pnpm dev

# Backend (FastAPI, если используется)
cd apps/api
uvicorn app.main:app --reload
```

## 📋 Workflows

### Workflow A: Meetings
Обработка встреч → Суммаризация → Извлечение участников → Генерация follow-up

### Workflow B: Tasks
Парсинг задачи → Извлечение deadline → Планирование уведомления

### Workflow C: Knowledge
Индексация документа → Векторизация → RAG поиск

## 🛠️ Технологический стек

- **Frontend**: Next.js 14, React, Tailwind CSS, Shadcn/UI
- **Backend**: Next.js API Routes + FastAPI (опционально)
- **AI**: LangChain, LangGraph, OpenAI GPT-4o
- **Vector DB**: ChromaDB (dev) / Pinecone (prod)
- **Database**: SQLite (dev) / PostgreSQL (prod)

## 📝 Статус проекта

🟡 **Planning Phase** — Архитектура и план готовы, разработка начнется с Phase 0.

---

**Last Updated**: 2025-01-20
