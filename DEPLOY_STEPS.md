# Пошаговая инструкция: Деплой через Vercel Dashboard

## ✅ Что уже сделано

Ты уже выполнил `git push` — код отправлен в GitHub репозиторий `exacttlyyou-prog/neuroslav`.

## 📋 Что делать дальше

### Шаг 1: Зайди на Vercel

1. Открой [vercel.com](https://vercel.com)
2. Войди в аккаунт (или зарегистрируйся через GitHub)

### Шаг 2: Создай новый проект

1. Нажми кнопку **Add New...** (в правом верхнем углу)
2. Выбери **Project**

### Шаг 3: Подключи GitHub репозиторий

1. Если GitHub еще не подключен:
   - Нажми **Import Git Repository**
   - Авторизуйся через GitHub
   - Разреши доступ к репозиториям
   
2. Найди репозиторий `exacttlyyou-prog/neuroslav` в списке
3. Нажми **Import** рядом с репозиторием

### Шаг 4: Настрой проект

Vercel автоматически определит настройки из `vercel.json`, но проверь:

- **Project Name:** `neuroslav-api` (или другое название)
- **Framework Preset:** Other
- **Root Directory:** `./` (корень проекта)
- **Build Command:** оставь пустым
- **Output Directory:** оставь пустым
- **Install Command:** оставь пустым

**Пока не добавляй Environment Variables** — сделаем это после первого деплоя.

### Шаг 5: Деплой

1. Нажми кнопку **Deploy**
2. Дождись завершения деплоя (обычно 1-2 минуты)
3. **Скопируй URL** из результата (например: `https://neuroslav-api.vercel.app`)

### Шаг 6: Добавь переменные окружения

1. В проекте на Vercel перейди в **Settings** → **Environment Variables**
2. Добавь переменные (кнопка **Add New**):

   ```
   TELEGRAM_BOT_TOKEN = твой_токен_от_BotFather
   ADMIN_CHAT_ID = твой_chat_id
   TELEGRAM_WEBHOOK_URL = https://твой-проект.vercel.app
   ```

   **Важно:** Для `TELEGRAM_WEBHOOK_URL` используй URL, который получил на шаге 5.

3. Добавь другие переменные из `.env` (если нужны):
   - `NOTION_TOKEN`
   - `NOTION_MCP_TOKEN`
   - и т.д.

4. Для каждой переменной выбери **Environment**: Production (и Development, Preview если нужно)

### Шаг 7: Redeploy с переменными

1. Перейди в **Deployments**
2. Найди последний деплой
3. Нажми на три точки (⋮) → **Redeploy**
4. Подтверди **Redeploy**

### Шаг 8: Проверка

1. Дождись завершения redeploy
2. Отправь сообщение боту в Telegram
3. Если бот отвечает — всё работает! ✅

Webhook настроится автоматически при старте приложения.

## 🔄 Обновление кода в будущем

После изменений в коде:

```bash
git add -A
git commit -m "Описание изменений"
git push
```

Vercel автоматически задеплоит изменения (если подключен GitHub).

Или вручную через Dashboard: **Deployments** → **Redeploy**

## ❓ Если что-то не работает

1. Проверь логи: **Deployments** → выбери деплой → **Functions** → выбери функцию → смотри логи
2. Проверь переменные окружения в **Settings** → **Environment Variables**
3. Проверь, что webhook настроен: отправь `/start` боту в Telegram
