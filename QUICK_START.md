# Быстрый старт

## 🚀 Автоматический запуск всей системы

Просто запустите один скрипт:

```bash
./launch.sh
```

Этот скрипт автоматически:
- ✅ Проверит все зависимости (Python, Node.js, Homebrew, Ollama)
- ✅ Установит BlackHole (если не установлен)
- ✅ Создаст/обновит виртуальное окружение Python
- ✅ Установит все зависимости
- ✅ Запустит Ollama (если не запущен)
- ✅ Запустит Backend (FastAPI на порту 8000)
- ✅ Запустит Frontend (Next.js на порту 3000)

**Остановка:** Нажмите `Ctrl+C` в терминале

## 🎙 Запуск записи встречи

После того, как система запущена:

```bash
./start_meeting.sh
```

Или вручную:
```bash
cd apps/api
source .venv/bin/activate
python scripts/record_meeting.py
```

## ⚙️ Первоначальная настройка (один раз)

### 1. Установка BlackHole и настройка Multi-Output Device

**macOS Tahoe:** Multi-Output Device создается через **Audio MIDI Setup**, а не через системные настройки!

1. Установите BlackHole (скрипт `launch.sh` сделает это автоматически, или вручную):
   ```bash
   brew install blackhole-2ch
   ```

2. Откройте **Audio MIDI Setup**:
   - Нажмите `Cmd+Space` (Spotlight)
   - Введите "Audio MIDI Setup" и нажмите Enter

3. Создайте Multi-Output Device:
   - В меню **Window** → **Show Audio Devices** (или `Cmd+2`)
   - Нажмите **+** (внизу слева)
   - Выберите **Create Multi-Output Device**
   - Отметьте галочками:
     - Ваши наушники/колонки (например, "AirPods Pro")
     - **BlackHole 2ch**
   - Закройте Audio MIDI Setup

4. В **Системных настройках** → **Звук** → **Выход** выберите созданный **Multi-Output Device**

### 2. Настройка .env файла

Создайте файл `apps/api/.env` с необходимыми переменными:

```env
# Ollama
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen3:8b

# Notion
NOTION_TOKEN=ntn_...
NOTION_MEETING_PAGE_ID=...

# Telegram
TELEGRAM_BOT_TOKEN=...
ADMIN_CHAT_ID=...
```

## 📋 Что запускается

После `./launch.sh` доступны:

- **Backend API**: http://localhost:8000
- **API Docs**: http://localhost:8000/docs
- **Frontend**: http://localhost:3000
- **Ollama**: http://localhost:11434

## 🔍 Проверка статуса

```bash
# Проверить все сервисы
cd apps/api
source .venv/bin/activate
python scripts/check_servers.py
```

## 🛠 Устранение проблем

### BlackHole не найден
```bash
brew install blackhole-2ch
# После установки может потребоваться перезагрузка Mac
```

### Ollama не запускается
```bash
ollama serve
```

### Порты заняты
```bash
# Проверить, что занимает порты
lsof -i :8000  # Backend
lsof -i :3000  # Frontend
lsof -i :11434 # Ollama
```

### Зависимости не установлены
```bash
# Python зависимости
cd apps/api
source .venv/bin/activate
pip install -r requirements.txt

# Node.js зависимости
cd apps/web
npm install
```
